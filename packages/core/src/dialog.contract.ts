/**
 * dialog.contract.ts — slot classes for the M3 (basic) Dialog.
 */
export interface DialogClasses {
  backdrop: string;
  popup: string;
  title: string;
  description: string;
  close: string;
}

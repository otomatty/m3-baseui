/**
 * slider.contract.ts — slot classes for the M3 Slider.
 */
export interface SliderClasses {
  root: string;
  control: string;
  track: string;
  indicator: string;
  thumb: string;
  value: string;
}

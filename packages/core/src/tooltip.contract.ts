/**
 * tooltip.contract.ts — slot classes for the M3 (plain) Tooltip.
 * Only the visible parts (popup, arrow) carry M3 styling.
 */
export interface TooltipClasses {
  popup: string;
  arrow: string;
}

/**
 * select.contract.ts — slot classes for the M3 Select.
 */
export interface SelectClasses {
  trigger: string;
  value: string;
  icon: string;
  popup: string;
  item: string;
  itemIndicator: string;
  groupLabel: string;
}
